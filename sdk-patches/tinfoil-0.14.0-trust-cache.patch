--- a/tinfoil/client.py
+++ b/tinfoil/client.py
@@ -587 +587 @@
-    def __init__(self, enclave: str = "", repo: str = DEFAULT_CONFIG_REPO, measurement: Optional[dict] = None, transport: TransportMode = DEFAULT_TRANSPORT_MODE, base_url: Optional[str] = None, attestation_bundle_url: str = "", user_cache_secret: Optional[str] = None):
+    def __init__(self, enclave: str = "", repo: str = DEFAULT_CONFIG_REPO, measurement: Optional[dict] = None, transport: TransportMode = DEFAULT_TRANSPORT_MODE, base_url: Optional[str] = None, attestation_bundle_url: str = "", user_cache_secret: Optional[str] = None, sigstore_verifier_factory=None):
@@ -626,0 +627 @@
+        self._sigstore_verifier_factory = sigstore_verifier_factory
@@ -929 +930,3 @@
-                    sigstore_bundle, digest, self.repo, release.tag
+                    sigstore_bundle, digest, self.repo, release.tag,
+                    verifier=(self._sigstore_verifier_factory()
+                              if self._sigstore_verifier_factory is not None else None),
@@ -968 +971,3 @@
-                bundle.sigstore_bundle, bundle.digest, self.repo, bundle.release_tag
+                bundle.sigstore_bundle, bundle.digest, self.repo, bundle.release_tag,
+                verifier=(self._sigstore_verifier_factory()
+                          if self._sigstore_verifier_factory is not None else None),
--- a/tinfoil/sigstore.py
+++ b/tinfoil/sigstore.py
@@ -134 +134 @@
-    bundle_json: bytes, digest: str, repo: str, expected_release_tag: Optional[str] = None
+    bundle_json: bytes, digest: str, repo: str, expected_release_tag: Optional[str] = None, *, verifier: Optional[Verifier] = None
@@ -154 +154,2 @@
-    verifier = Verifier.production()
+    if verifier is None:
+        verifier = Verifier.production()
@@ -197 +198 @@
-    bundle_json: bytes, digest: str, repo: str, expected_release_tag: Optional[str] = None
+    bundle_json: bytes, digest: str, repo: str, expected_release_tag: Optional[str] = None, *, verifier: Optional[Verifier] = None
@@ -215 +216 @@
-        statement = _verify_dsse_bundle(bundle_json, digest, repo, expected_release_tag)
+        statement = _verify_dsse_bundle(bundle_json, digest, repo, expected_release_tag, verifier=verifier)
